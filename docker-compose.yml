services:
  postgress:
    image: postgres:latest
    container_name: postgress
    env_file:
      - ./.env 
    ports:
      - "5432:5432"
    volumes:
      - postgresData:/var/lib/postgresql
volumes:
  postgresData: